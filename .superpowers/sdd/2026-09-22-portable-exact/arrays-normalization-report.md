# Dense indexed arrays

Added typed dense array replacement, append, contiguous nonnegative literal element assignment, literal element reads, element-count reads, scalar element-zero reads/writes, and one quoted array splice with prefix/suffix. The normalizer tracks known lengths; uncertain branch/call shapes retain their array identity with sentinel -1 and are rejected when a proof is needed. Source entry facts carry shapes. Function bodies cannot assume definition-time array contents. Loop mutations, sparse/associative construction, dynamic indices, index-list expansions, array exports/declarations and unquoted splices remain excluded. Root owns structural list materialization, array entry guards and framed child vector snapshots.

The initial four CLI red probes rejected T_Array as expected. Canonical GHC9.14.1 build-all passes. Focused canonical arrays/jobs run: **39/40 pass**; every byte/status execution comparison and all six new wait diagnostics pass. The one negative failure exposed machine-Int wrapping for a huge index. Source now parses via unbounded Integer and checks maxBound before conversion; final recompilation is pending concurrent Task6 plan integration. Added equal-branch-shape and multiple-array/argv-empty snapshot regressions for that next run.

Broad current binary: **806/811 pass**, with the known overflowing-index negative plus four stale expectations (array append exclusion, two diagnostic codes, assignments golden exclusion). Those stale expectations are updated; original Bash fixture bytes remain unchanged. Full suite observed no Bash/Fish behavioral mismatch. Logs `/tmp/monk-canonical-arrays-build.log`, `/tmp/monk-canonical-arrays-focused.log`, `/tmp/monk-canonical-arrays-broad.log`.

Known entry condition: names used as arrays must be absent in the inherited environment. Runtime checks reject before script effects. Tests cover this boundary, scalar-to-array conversion, element-zero scalar writes, empty and newline elements, invalid UTF8, snapshots, mutation isolation and cardinality. No commits; root owns final audit/todo.

Later canonical build-all passes with the input/trap/word/prefix frontend integrated. Focused **98/98** arrays/input/trap tests pass; array overflow rejection and additional vector snapshot checks are green. Root resolved the Unicode echo oracle by selecting the pinned custom Bash build configuration. The comprehensive final run is still pending recursive-composition/runtime process-owner stabilization. See `input-traps-expansion-report.md`.
